# Local Tools

Local tools and executables are not stored in Git. The developer may create `Tools/Godot/` locally for a local Godot installation.

When possible, both the VPS and the local computer should use Godot 4.7.2 Standard. Godot executable files must never be committed.

The actual game project is inside `Game/`.

